/**
 * Using a Full HD resolution (1.77 ratio) as base
 * https://en.wikipedia.org/wiki/16:9_aspect_ratio
 */

const BASE_WIDTH = 800;
const BASE_HEIGHT = 600;
export const BASE_WINDOW_SIZE = {
  BASE_WINDOW_WIDTH: BASE_WIDTH,
  BASE_WINDOW_HEIGHT: BASE_HEIGHT,
  BASE_WINDOW_RATIO: BASE_WIDTH / BASE_HEIGHT
};
